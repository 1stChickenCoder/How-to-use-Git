## Tránh sự theo dõi của Git bằng `.gitignore`

Tạo file `.gitignore` trong repo
Nếu muốn git không theo dõi file hay folder nào thì thêm vào

```bash
node_modules/** // bỏ theo dõi cả folder này
!node_modules/app.js // ngoại trừ app.js
dist/       // bỏ theo dõi folder này
style.css   // bỏ theo dõi file này
```

Ngoài ra đặt luật thêm thì hỏi AI

Các phần tử đã bị ignore thì sẽ bị làm mờ trong treeview, nếu bình thường thì vẫn sáng bình thường.