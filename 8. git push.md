## Đưa code lên remote repo bằng `git push`

Trước tiên thì vẫn cần đảm bảo môi trường Git đã liên kết đã nhé

`$ git init`
`$ git config`
`$ git commit`

Sau đó thì đưa code lên thôi. Thêm vào nhánh nào thì viết vào sau 'origin'
`$ git push origin main`
