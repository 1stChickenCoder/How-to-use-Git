## Theo dõi các commit thông qua `git log`

`$ git log`

Một số phím chức năng có thể dùng để điều hướng và tìm kiếm trong log

- return: dòng tiếp theo
- w: trang tiếp theo
- spacebar: trang trước
- q: thoát
- ?pattern: tìm kiếm, với pattern là mẫu tìm kiếm
- /pattern: giống ?pattern
- n: đến vị trí tìm kiếm phía dưới
- N: đến vị trí tìm kiếm ohias trên

Mỗi một commit có một mã hash đại diện

Để in log với mã hash ngắn gọn

`$ git log --oneline`